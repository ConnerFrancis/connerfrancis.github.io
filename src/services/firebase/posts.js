export default {
  ref: db.ref('posts'),
  
  /*add (board, author, content, image = false, timestamp = Date.now()) {
    window.alert(ref)
    
    firebase.database().ref('posts/' + id).set({
      author,
      board,
      content,
      image,
      timestamp
    })
  }*/
}